interface:
  display_name: "Health City"
  short_description: "Repo-specific patterns and workflows for Health-city"
  default_prompt: "Use the Health-city repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true